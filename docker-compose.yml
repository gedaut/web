services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: gedaut-web:latest
    container_name: gedaut_web
    ports:
      - "[::]:3000:3000"
    environment:
      - RUST_LOG=info
      - LEPTOS_SITE_ADDR=0.0.0.0:3000
    restart: unless-stopped