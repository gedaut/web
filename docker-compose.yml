services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: gedaut-web:latest
    container_name: gedaut_frontend
    ports:
      - "8080:8080"
    environment:
      - RUST_LOG=info
      - LEPTOS_SITE_ADDR=0.0.0.0:8080
    restart: unless-stopped